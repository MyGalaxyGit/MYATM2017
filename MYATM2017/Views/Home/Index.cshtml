@{
    ViewBag.Title = "Home Page";
}


<div class="row">
    <div class="col-md-6 margin-top-20"><a href="#" class="btn btn-primary btn-block"><span class="glyphicon glyphicon-circle-arrow-down"></span>Withdraw</a>
        <a href="#" class="btn btn-primary btn-block"><span class="glyphicon glyphicon-flash"></span>Quick Cash $100</a>
        <a href="@Url.Action("Details","CheckingAccount",new { checkingAccountId = ViewBag.CheckingAccountId })" class="btn btn-primary btn-block"><span class="glyphicon glyphicon-question-sign"></span>Balance Enquiry</a>
    </div>
    <div class="col-md-6 margin-top-20">
        <a href="#" class="btn btn-primary btn-block"><span class="glyphicon glyphicon-print"></span>Print Statement</a>
        <a href="#" class="btn btn-primary btn-block"><span class="glyphicon glyphicon-transfer"></span>Transfer Funds</a>
        <a href="@Url.Action("Deposit","Transaction",new { checkingAccountId = ViewBag.CheckingAccountId })" class="btn btn-primary btn-block"><span class="glyphicon glyphicon-circle-arrow-up"></span>Deposit</a>
    </div>
</div>

@*You are using @Request.Browser.Browser, and that's ok.!! Email us at support@MVCATM.com if yu have any problems. or send us a tweet @@mvcatm.

@{
    var amounts = new List<Decimal> { 100, 25.5m, -40.2m, 23, -2.23m, 98 };
    foreach (var item in amounts)
    {
        <ul>
            <li>@formatAmount(item)</li>
        </ul>
    }


}



@helper formatAmount(decimal  amount) {
var color = "green";
if (amount<0)
{
    color = "red";
}

<span   style=" color:@color">@String.Format("{0:c}",amount)</span>

    }*@